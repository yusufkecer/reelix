import 'package:flutter/material.dart';

final class AppRadius extends BorderRadius {
  const AppRadius.eighteen() : super.all(const Radius.circular(18));
  const AppRadius.thirtyOne() : super.all(const Radius.circular(31));
}
